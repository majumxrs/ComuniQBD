@model IEnumerable<ComuniQBD.Models.TipoCampanha>

@{
    ViewData["Title"] = "Index";
    <link rel="stylesheet" href="~/css/TipoCampanha.css" asp-append-version="true" />
}


<h1>Tipo Campanha</h1>

<p>
    <a asp-action="Create">Criar Novo</a>
</p>
<div class="table-container">
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoCampanhaNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
            <tr class="tabelinha">
            <td>
                @Html.DisplayFor(modelItem => item.TipoCampanhaNome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoCampanhaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.TipoCampanhaId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoCampanhaId">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>